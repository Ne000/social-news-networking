
<div class="col-md-8">
    <%= form_for [topic, post, post.comment.new] do |f| %>
        <div class="form-group">
            <%= f.label :body %>
            <%= f.text_area :body, rows: 2, class: 'form-control', placeholder: "Enter comment" %>
        </div>
        <%= f.submit "Create comment", class: 'btn btn-success' %>
    <% end %>
</div>
